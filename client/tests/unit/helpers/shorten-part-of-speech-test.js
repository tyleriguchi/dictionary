import { shortenPartOfSpeech } from '../../../helpers/shorten-part-of-speech';
import { module, test } from 'qunit';

module('Unit | Helper | shorten part of speech');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = shortenPartOfSpeech(42);
  assert.ok(result);
});
