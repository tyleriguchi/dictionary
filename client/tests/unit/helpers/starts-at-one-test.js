import { startsAtOne } from '../../../helpers/starts-at-one';
import { module, test } from 'qunit';

module('Unit | Helper | starts at one');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = startsAtOne(42);
  assert.ok(result);
});
