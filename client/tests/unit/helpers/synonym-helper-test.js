import { synonymHelper } from '../../../helpers/synonym-helper';
import { module, test } from 'qunit';

module('Unit | Helper | synonym helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = synonymHelper(42);
  assert.ok(result);
});
